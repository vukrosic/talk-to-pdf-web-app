// import React from 'react';
// import { Button } from 'antd';

// const AnswerOptions = ({ options, onClick }) => {
//   return (
//     <div>
//       {options.map((option, index) => (
//         <Button key={index} onClick={() => onClick(option)}>
//           {option.label}
//         </Button>
//       ))}
//     </div>
//   );
// };

// export default AnswerOptions;